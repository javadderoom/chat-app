// Database initialization script
// This is a helper script - use 'npm run db:push' to push schema to database
// or 'npm run db:generate' to generate migrations, then 'npm run db:migrate' to run them

console.log('To initialize the database, run one of the following:');
console.log('  npm run db:push      - Push schema directly to database (development)');
console.log('  npm run db:generate  - Generate migration files');
console.log('  npm run db:migrate   - Run migrations');
console.log('');
console.log('Or use Drizzle Studio to manage your database:');
console.log('  npm run db:studio');

